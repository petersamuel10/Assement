package com.church.ministry.util

enum class CodingKeys(var key: String) {

    LANGUAGE("language"),
    ACCESS_TOKEN("access_token"),
    MESSAGE_KEY("message"),
    USER("user"),
    USER_LOGGED("user_logged")

}